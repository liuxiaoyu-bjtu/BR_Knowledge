# 2026-08-14 工作日志

## 检测模型类别能力确认（default = COCO 80 类）

用户从 `default` 对应的 ONNX 模型中导出了完整类别表，确认通用物体检测模型为标准 **COCO 80 类**目标检测（YOLO 系），解决了之前"黑盒探测模型类别"的遗留问题。

### 三个检测模型的最终结论
- `default`：COCO 80 类（class_id 0-79），完整清单已沉淀到 booster-sdk.md §3.3
- `person`：人物检测（单一类别）
- `soccer`：RoboCup 场景检测（知识库 robocup-demo.md 有依据）

### 知识库更新
1. `booster-sdk.md` — 新增 §3.3「可用检测模型与类别能力」：模型总表 + COCO 80 类 4 列表格 + 课程应用提示；修正示例代码 `soccer_yolo` → `soccer`
2. `probe_detection_classes.py` — 注释更新为"验证实际输出是否符合预期"（类别已不再需要从零摸索）
3. `sdk-tests/_README.md` — probe 脚本说明同步
4. `INDEX.md` — 新增「COCO 80 类」「视觉检测模型」关键词
5. `CHANGELOG.md` — v1.16

### 关键知识点
- `DetectionResult.class_id` 即 COCO 类别索引，0=person, 41=cup, 39=bottle, 32=sports ball, 56=chair, 60=dining table
- 实验室常见可识别目标：人/杯子/瓶子/手机/椅子/桌子/球/书本/背包/键盘/鼠标/显示器/盆栽
- 课程视觉实验可直接按 COCO 80 类设计（找杯子/避让行人/识别课桌等）
