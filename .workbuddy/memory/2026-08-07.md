# 2026-08-07 工作日志

## 1. BoosterOS SDK 视觉能力梳理
- 读取 `01-booster-kb/07-tech-platform/booster-sdk.md` 全文，提取并系统化列出全部视觉相关能力
- 分类为 4 大类：图像采集（3 接口）、视觉检测（4 接口）、视觉数据类型（7 个）、视觉支撑能力（4 个）
- 绘制了完整视觉数据流管线：get_image → Detection.detect → get_camera_info + get_transform → set_head_angle → SoccerKickManager.update_ball

## 2. 编写 BoosterOS SDK 环境检测脚本
- 脚本路径：`04-shared/test_boosteros_sdk.py`
- 4 项测试：包安装检测 → 虚拟机器人连接 → 检测模型列表 → 图像采集验证
- 关键设计：全程只创建一个 BoosterRobot 实例并在各测试间传递复用（SDK 规定同一台机器人不应重复创建实例）
- 检测模型列表通过 `Detection.list_models()` 类方法获取，防御性解析 dict/非 dict 两种返回格式
- 脚本可在虚拟仿真环境或真机环境运行，包含详细错误提示和排查建议
